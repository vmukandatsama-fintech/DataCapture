# Copy this to config/local_settings.py on the server and fill in values.
# This file is gitignored — never commit local_settings.py itself.

DEBUG = False

SECRET_KEY = 'replace-with-a-long-random-string'

ALLOWED_HOSTS = ['192.168.10.28', 'localhost', '127.0.0.1']
